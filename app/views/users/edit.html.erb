<% provide :title, "ユーザー情報の編集" %>
<header>
  <div class="header-content">
    <div class="form-wrapper" style="padding-left:2px;">
      <h2>ユーザー情報編集</h2>
      <%= form_for @user do |f| %>
        <div class="form-item">
          <%= f.label "苗字" %>
          <%= f.text_field :family_name, :class => "form-control" %>
        </div>
        <div class="form-item">
          <%= f.label "名前" %>
          <%= f.text_field :first_name, :class => "form-control" %>
        </div>
        <div class="form-item">
          <%= f.label "プロフィール画像" %>
          <%  if @user.image.present?  %>
            <%= image_tag @user.authenticated_image_url(:thumb), :class => "img-responsive", :alt => "" %>
          <% end %>
          <%= f.file_field :image, :class => "form-controll" %>
        </div>
        <div class="form-item">
          <%= f.label "23区名" %>
          <%= f.collection_select :ward_id, Ward.all, :id, :name, {:selected => @ward.id }, {:class => "form-control"}  %>
        </div>
        <div class="form-item">
          <%= f.label "駅名" %>
          <%= render partial: 'stations', locals: { ward_id: @ward.id} %>
        </div>
        <br>
        <div class="actions">
          <%= f.submit "編集する",:class => 'btn btn-primary scroll-page' %>
        </div>
      <% end %>
    </div>
  </div>
</header>